<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact_path(contact) %> <%= link_to "delete", contact_path(contact), data: {confirm: "you sure?", method: "delete"}  %></li>
  <% end %>
</ul>

<%= link_to "new", new_contact_path  %>
